{
  "name": "devious-sandbox",
  "version": "0.0.0",
  "description": "A devious side project",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/devious-design/devious-sandbox.git"
  },
  "author": "Stephen Esser",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/devious-design/devious-sandbox/issues"
  },
  "homepage": "https://devious-design.github.io/devious-sandbox/",
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "lint": "npm run lint:scss && npm run lint:js",
    "lint:js": "eslint --ext .js,.jsx .",
    "lint:scss": "stylelint src/**/*.scss",
    "test": "react-scripts test",
    "clean": "rm -rf node_modules && rm -rf build",
    "clean:install": "npm run clean && npm install",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "browserslist": [
    "extends browserslist-config-terra"
  ],
  "eslintConfig": {
    "extends": "terra"
  },
  "stylelint": {
    "extends": "stylelint-config-terra"
  },
  "dependencies": {
    "classnames": "^2.2.6",
    "gh-pages": "^2.0.1",
    "node-sass": "^4.12.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-intl": "^2.9.0",
    "react-scripts": "3.0.1",
    "terra-base": "^5.11.0"
  },
  "devDependencies": {
    "browserslist-config-terra": "^1.2.0",
    "eslint": "^5.16.0",
    "eslint-config-terra": "^2.5.0",
    "stylelint": "^10.1.0",
    "stylelint-config-terra": "^3.1.0"
  }
}
